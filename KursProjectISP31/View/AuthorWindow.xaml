<Window x:Class="KursProjectISP31.View.AuthorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursProjectISP31.View"
        xmlns:vm="clr-namespace:KursProjectISP31.ViewModel"
        mc:Ignorable="d"
        Title="Добавление автора" Height="650" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding Surname}" Grid.Row="0" Margin="10 30"/>
        <TextBox Text="{Binding FirstName}" Grid.Row="1" Margin="10 30"/>
        <TextBox Text="{Binding LastName}" Grid.Row="2" Margin="10 30"/>
        <TextBox Text="{Binding Address}" Grid.Row="3" Margin="10 30"/>
        <TextBox Text="{Binding Phone}" Grid.Row="4" Margin="10 30"/>
        <TextBox Text="{Binding AdditionalInformation}" Grid.Row="5" Margin="10 30"/>
        <Grid Grid.Row="6">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Content="Ok" Grid.Column="0" Margin="10 20"/>
            <Button Content="Cancel" Grid.Column="1" Margin="10 20"/>
        </Grid>
        
    </Grid>
</Window>
